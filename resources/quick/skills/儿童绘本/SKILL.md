---
name: "儿童绘本"
description: "处理儿童绘本网页相关任务：可直接生成可运行的 HTML，也可回答逻辑原理、排查问题、微调现有实现。当用户提到开发/修改/解释儿童绘本网页时触发。"
---

# 儿童绘本网页技能

当用户想要“开发一个儿童绘本的网页”“做一个儿童绘本 HTML 页面”“生成一个儿童绘本演示网页”“做一个绘本故事网站/H5/落地页”时，触发这个技能。

这个技能不是只给方案，而是要么直接产出一个可运行的网页，要么围绕现有儿童绘本网页回答逻辑、原理、接口链路、报错原因，并在需要时对现有实现做最小改动。

## 功能边界

这个技能实际承担两类任务，必须都能处理：

### A. 直接生成网页

当用户要“开发一个儿童绘本的网页”“做一个儿童绘本 HTML 页面”“生成一个儿童绘本演示页”时：

- 直接创建或修改网页文件
- 默认最终交付为 `website/index.html`
- 默认产出可运行 HTML，而不是停留在方案或伪代码

### B. 回答逻辑 / 原理 / 微调现有实现

当用户并不是让你从零生成，而是在问：

- 这个网页的逻辑是什么
- 为什么这里失败了
- 轮询是不是超时了
- 这一步接口该怎么调
- 帮我微调这个 prompt
- 帮我改一下已有的策略/字段/展示

此时本技能也必须触发，并具备：

- 阅读现有实现并解释逻辑
- 结合接口返回分析报错原因
- 对已有 HTML 做最小改动
- 回答“原理、链路、字段含义、为什么这么设计”

## 参考文件使用规则

如果技能目录下存在参考实现，必须优先读取并吸收，而不是重新凭空设计。

当前参考文件：

- `website/index.html`

使用规则：

1. 当用户要生成儿童绘本网页时，优先参考 `website/index.html` 的结构、字段命名、步骤拆分、接口调用方式和展示区布局。
2. 当用户要修改已有网页时，优先沿用参考实现和现有实现中的命名习惯、状态流转、字段提取逻辑，按最小 Diff 修改。
3. 当用户问“具体逻辑/原理/为什么失败”时，优先从 `website/index.html` 中提取真实代码逻辑来回答，不要只给抽象解释。
4. 如果参考实现和用户当前文件存在差异，优先以用户当前文件为准，再用参考实现补全缺失逻辑。

## 触发条件

当用户出现以下任一意图时，应优先使用本技能：

- 开发一个儿童绘本的网页
- 做一个儿童绘本 HTML 页面
- 生成一个儿童故事绘本演示页
- 做一个绘本故事视频生成网页
- 基于儿童绘本主题生成网页工具
- 做一个给儿童绘本/故事口播/分镜生成用的前端页面
- 帮我解释这个儿童绘本网页的逻辑
- 帮我看这个儿童绘本网页为什么报错
- 帮我调整这个儿童绘本页面的 prompt / 接口 / 展示 / 轮询策略

如果用户同时提到以下关键词，也应视为强触发：

- `儿童绘本`
- `绘本故事`
- `儿童故事网页`
- `HTML`
- `index.html`
- `H5`
- `演示页`
- `网页工具`
- `逻辑`
- `原理`
- `为什么失败`
- `微调`
- `报错`
- `轮询`

## 核心目标

根据用户需求，生成一个完整、可运行、可直接打开的网页，优先满足以下要求：

1. 页面可直接落地为 HTML 文件。
2. 页面结构完整，至少包含输入区、操作区、状态区、结果区。
3. 如用户需求涉及接口调用，页面需包含可执行的前端逻辑，而不是只写静态示意。
4. 如需求不完整，优先做“默认可用版本”，避免停留在抽象方案。
5. 若用户在问已有页面的逻辑、原理、报错或微调策略，则必须基于真实代码回答，不要脱离当前实现空谈。

## 默认产出规则

若用户没有额外指定，默认按以下方式实现：

- 产出单文件 `website/index.html`
- 内联 `CSS` 与 `JavaScript`
- 中文界面
- 现代卡片式布局
- 响应式基础适配
- 提供必要的运行说明
- 失败必须能看到错误详情（包含 phase、task_id、url、最后一次响应）
- 轮询超时时间默认 20 分钟（可由用户要求调整）
- 如存在参考实现，优先吸收 `website/index.html`
- 如用户提供现成页面，优先按最小改动方式迭代
- 默认提供 5 个可点击的儿童绘本主题模板，方便新用户快速体验

## 儿童绘本网页的推荐默认结构

除非用户另有指定，儿童绘本类网页默认建议包含以下模块：

### 1. 输入区

- 绘本主题输入
- API Token 输入
- 可选风格、角色、分镜数配置
- 推荐附带 5 个默认主题模板按钮，点击即可自动填入主题

### 2. 处理步骤区

以可视化步骤展示链路，例如：

1. 生成分镜文案
2. 生成配音
3. 生成字幕时间轴
4. 生成配图
5. 组装草稿或导出结果

### 3. 结果展示区

- 完整口播文案
- 分镜列表
- 每个分镜的配图提示词
- 字幕时间轴
- 图片或素材结果
- 草稿 ID / 下载链接 / 预览信息

### 4. 状态反馈

需要清晰展示：

- 等待中
- 进行中
- 已完成
- 失败

## 当需求涉及“儿童绘本视频生成网页”时的默认实现策略

1. 用户输入儿童绘本主题
2. 调用大模型生成结构化分镜 JSON
3. 生成整段口播文案
4. 调用 TTS 生成音频
5. 调用字幕模板接口，拿到精确时间戳（`output.subtitles`）
6. 基于分镜提示词，调用 AI 生图接口生成 9:16 竖屏图片
7. 以字幕时间轴为准，把图片从 0 秒连续铺满到最后一句字幕结束（中间不能有空隙）
8. 给每个“前置图片”随机添加转场（候选：向左/叠化/向右/左移/右移/翻页/眨眼；最后一张通常不加）
9. 输出草稿 ID、下载链接与可视化结果

## 结构化分镜生成要求

若用户需要“文案 + 分镜 + 配图提示词”，优先让模型返回严格 JSON，而不是自由文本。

推荐返回结构：

```json
{
  "title": "故事标题",
  "script_text": "完整口播文案",
  "scenes": [
    {
      "title": "分镜标题",
      "narration": "该分镜对应口播",
      "image_prompt": "该分镜配图提示词",
      "visual_focus": "画面重点"
    }
  ]
}
```

要求：

- `script_text` 为完整口播
- `scenes[].narration` 按顺序拼接后应与完整口播一致或高度一致
- `image_prompt` 必须可直接用于生图
- 画风、角色、场景前后一致
- 口播节奏应更像“绘本老师讲故事”，比普通慢读略快，避免拖沓
- 单句尽量短促顺口，建议控制在 8 到 18 个字
- 尽量少用省略号、破折号、连续感叹词和过多停顿词，避免 TTS 听感过慢
- 可包含轻量环境音提示、背景氛围提示和少量人物对白，但仍以温柔旁白为主

## 默认主题模板要求

若用户没有提供自己的示例主题，页面默认建议提供 5 个可点击主题模板。

模板要求：

- 一键点击即可填入主题输入框
- 适合低龄儿童理解
- 题材尽量覆盖：勇气、情绪管理、冒险、想象力、上台成长
- 文案长度适中，便于新用户直接点按钮体验整条链路

推荐模板示例：

- 一只怕黑的小兔子在萤火虫和妈妈的陪伴下，学会勇敢面对夜晚，适合 3-6 岁儿童
- 一只爱发脾气的小狮子在森林朋友的帮助下学会表达情绪，温暖治愈，适合 4-7 岁儿童
- 小海龟第一次离开家去看大海深处，在冒险中学会独立与分享，适合 4-8 岁儿童
- 一个爱问为什么的小女孩和会说话的月亮一起夜游城市，充满想象力与安全感，适合 5-8 岁儿童
- 森林里胆小的小刺猬参加星光音乐会，在伙伴鼓励下勇敢上台，适合 3-6 岁儿童

## 配音节奏要求

当用户希望“说话频率更高”“更像讲故事”“不要那么慢”时，默认优先从两层处理：

### 1. 文案生成层

- 在分镜文案 prompt 中明确要求：
  - 口播更像讲故事
  - 节奏更紧凑
  - 句子更短
  - 推进感更强
  - 避免过多停顿型标点

### 2. TTS 输入整理层

- 在调用 `seed_audio` 前，对 `script_text` 做一次轻量清洗，生成 `ttsText`
- 典型处理包括：
  - 去除多余换行
  - 将重复省略号替换为较短停顿
  - 将过长破折号替换为逗号
  - 合并重复标点
  - 压缩多余空格

目标：

- 不改变原故事内容
- 只优化 TTS 的听感节奏
- 让成品更接近“儿童故事讲述”而非“抒情慢读”

## 字幕时间轴要求

当用户提到“字幕时间”“每句话时间戳”“按字幕铺图”时：

- 优先使用字幕模版接口返回的精确时间轴
- 重点读取 `output.subtitles`
- 每条字幕通常包含：
  - `text`
  - `start_time`
  - `end_time`
  - `target_track`

如果拿到了 `output.subtitles`，则后续图片时段必须严格以这组时间为准，不要自行重算总时长覆盖真实结果。

## 图片生成与铺图规则

当用户要求按分镜生成图片时，默认遵守：

- 使用用户指定模型；若未指定，可使用项目中既定方案
- 若用户明确要求 9:16，则使用竖屏尺寸
- 每张图片对应一个连续时间片段
- 全部图片必须从 `0` 秒连续铺到最后一句字幕结束
- 中间不能出现空白时间段

若用户额外要求转场，则：

- 给每个前置图片设置转场
- 最后一张通常不设置转场
- 按用户给定候选集随机选择

## 输出质量要求

生成网页代码时，遵循以下要求：

- 代码应完整可运行，不要只给片段
- 文案、样式、脚本之间要闭合
- DOM id/class 与脚本引用一致
- 状态流转要自洽
- 错误提示要可读
- 优先最小依赖，避免不必要框架

## 默认交付格式

默认直接生成：

- 一个完整的 `website/index.html`

若用户明确要求，也可以生成：

- `website/index.html`
- `style.css`
- `script.js`

但若未明确说明，优先单文件方案，便于用户直接打开验证。

## 回答与执行方式

使用本技能时，应优先：

1. 明确用户想要的是展示页，还是可调用接口的工具页。
2. 如果信息不足但可合理默认，则直接开始实现。
3. 直接生成网页文件内容，而不是停留在产品方案描述。
4. 若已有现成 `website/index.html`，则按最小改动方式迭代。

## 工作模式判断

收到请求后，先判断属于以下哪种模式：

### 模式 1：生成模式

适用于：

- 从零开发儿童绘本网页
- 生成 HTML 演示页 / 工具页
- 让页面直接可调用接口

执行要求：

- 优先读取 `website/index.html`
- 若用户已有目标文件，则直接在目标文件上迭代
- 最终产出可运行 HTML

### 模式 2：问答模式

适用于：

- 用户问逻辑、原理、链路
- 用户问“为什么失败”“是不是轮询超时”
- 用户问某个接口怎么工作
- 用户问某个字段是什么意思

执行要求：

- 优先阅读当前真实文件或 `website/index.html`
- 回答时以真实实现为依据
- 先给结论，再给依据，再给建议
- 如果能从代码里定位到阶段（如 `storyboard / tts / subtitle / image`），要明确指出

### 模式 3：微调模式

适用于：

- 调整 prompt
- 增加模板
- 修改轮询时间
- 调整展示区
- 对已有生成策略、字段映射、错误展示做小改动

执行要求：

- 基于现有 HTML 做最小化修改
- 不要重写整页，除非用户明确要求
- 修改后尽量保留现有 DOM 结构、字段名和状态机

## 逻辑解释要求

如果用户问“逻辑是什么”或“原理是什么”，应至少覆盖：

1. 页面输入了什么
2. 调用了哪些接口
3. 每一步的输入输出是什么
4. 异步任务如何轮询
5. 哪些字段是关键字段
6. 哪一步最容易失败以及失败时如何判断

优先按以下顺序解释：

1. 创建草稿
2. 生成分镜文案
3. 生成 TTS 音频
4. 生成字幕时间轴
5. 生成图片并写回草稿
6. 下载最新草稿

## 报错分析要求

如果用户给出截图、报错文案、错误 JSON，或问“是不是轮询超时了”，必须：

1. 判断报错属于哪个 phase：`create_draft / storyboard / tts / subtitle / image`
2. 判断是：
   - HTTP 请求失败
   - 异步任务状态失败
   - 轮询超时
   - 成功但字段解析错误
3. 明确告诉用户“是不是超时”
4. 如果页面错误展示不够详细，应优先补充错误详情输出，而不是只口头猜测

## 微调策略要求

如果用户让你改已有策略，例如：

- 多加几个默认主题模板
- 调整文案风格
- 让配音更有环境音/背景音/多人物对白
- 让说话频率更高、更像讲故事
- 修改字幕逻辑
- 改成 20 分钟超时
- 生图增加转场

应优先：

1. 找到真实对应函数或常量
2. 只改与该需求直接相关的逻辑
3. 尽量不改无关 UI 和其他阶段
4. 修改后说明“改了什么、影响哪一步、用户现在该如何验证”

## 示例触发语句

以下说法都应触发本技能：

- “开发一个儿童绘本的网页”
- “帮我做一个儿童绘本 HTML 页面”
- “写个儿童故事绘本生成网页”
- “做个儿童绘本演示页，最后输出 HTML”
- “我要一个儿童绘本视频生成工具网页”
- “你看下这个儿童绘本网页的逻辑”
- “这里为什么失败了”
- “是不是轮询超时了”
- “帮我改一下这个儿童绘本网页的策略”

## 示例结果

本技能的理想结果应类似：

- 创建或修改 `website/index.html`
- 页面可以直接在浏览器打开
- 用户可输入儿童绘本主题并点击生成
- 页面可展示文案、分镜、字幕时间轴、图片结果
- 若接了接口，可直接生成草稿并提供下载链接
- 若用户是在提问，则能直接解释当前实现的逻辑、原理、错误原因和改法

## 接口参考（必须按此对齐）

当用户要求网页“能跑通接口链路”，生成的 HTML 应内置以下接口调用（或按用户要求替换为对应环境地址）。所有请求均使用：

- Header：`Authorization: Bearer <token>`
- Header：`Content-Type: application/json`

### 1) 创建草稿

- URL：`POST https://open.vectcut.com/cut_jianying/create_draft`
- Body 示例：

```json
{
  "width": 1080,
  "height": 1920,
  "name": "儿童绘本：一只怕黑的小兔子学会勇敢"
}
```

- 关键返回字段：`output.draft_id`

### 2) 生成分镜文案（异步对话任务）

- URL：`POST https://open.vectcut.com/llm/chat/submit_task/submit_chat_task`
- 要求：让模型输出“严格 JSON”（建议 `response_format: "json"`）
- Body 示例：

```json
{
  "system_prompt": "你是一个擅长儿童绘本视频策划的中文编剧和分镜导演。",
  "user_input": "请为儿童绘本短视频生成一个严格可解析的 JSON 对象……",
  "model": "qwen3.6-plus",
  "response_format": "json",
  "stream": false
}
```

- 返回：`task_id`

### 3) 查询分镜文案任务状态

- URL：`GET https://open.vectcut.com/llm/chat/submit_task/task_status?task_id=<task_id>`
- 关键返回字段（常见之一）：
  - `result.assistant`
  - 或 `result.response.choices[0].message.content`

### 4) 文案转配音（TTS）

- URL：`POST https://open.vectcut.com/llm/tts/seed_audio/generate`
- 推荐策略：优先传入经过轻量节奏整理后的 `ttsText`，而不是未经处理的原始长文本
- Body 示例：

```json
{
  "model": "seed-audio-1.0",
  "text_prompt": "……完整口播文案……",
  "voice_id": "gv_2e601fae38484816adf8bf5c38b79393"
}
```

- 关键返回字段（常见之一）：`output.url` 或 `output.audio_url`

### 5) 字幕模版（关键：返回精确时间戳）

- URL：`POST https://open.vectcut.com/cut_jianying/generate_smart_subtitle`
- Body 示例：

```json
{
  "agent_id": "asr_42da310c1e4347ddb2c96dd2a5d055c2",
  "draft_id": "dfd_xxx",
  "url": "https://xxx.wav",
  "add_media": true,
  "text_content": "……完整口播文案……"
}
```

- 返回：`task_id`

### 6) 查询字幕任务状态（关键：output.subtitles）

- URL：`GET https://open.vectcut.com/cut_jianying/smart_subtitle_task_status?task_id=<task_id>`
- 成功示例结构（重点字段）：

```json
{
  "success": true,
  "status": "success",
  "output": {
    "draft_id": "dfd_cat_xxx",
    "draft_url": "https://www.vectcut.com/draft/downloader?draft_id=dfd_cat_xxx&is_capcut=0",
    "subtitles": [
      {
        "id": "sub_001",
        "text": "家人们",
        "start_time": 0.5,
        "end_time": 0.98,
        "target_track": "track_sub"
      }
    ]
  }
}
```

- 规则：后续铺图必须以 `output.subtitles[].start_time/end_time` 为唯一时间轴来源。

### 7) AI 生图（gpt-image-2-all，9:16）

- URL：`POST https://open.vectcut.com/llm/image/submit_task/generate`
- 关键点：
  - `model: "gpt-image-2-all"`
  - 9:16 推荐 `size: "941x1672"`（或按文档可用的 9:16 尺寸）
  - `compose_draft: true`，并带 `draft_id`、`start`、`end` 写回草稿
  - 转场使用 `transition` 字段
- Body 示例：

```json
{
  "prompt": "温暖童趣的绘本插画风格……9:16竖屏……",
  "model": "gpt-image-2-all",
  "size": "941x1672",
  "compose_draft": true,
  "draft_id": "dfd_cat_xxx",
  "start": 0,
  "end": 3.2,
  "track_name": "main",
  "transition": "叠化",
  "transition_duration": 0.45
}
```

### 8) 查询生图任务状态

- URL：`GET https://open.vectcut.com/llm/image/submit_task/task_status?task_id=<task_id>`
- 关键返回字段：`result.image`（图片 URL），以及 `result.draft_id`（草稿可能会更新）

## 轮询与错误处理（必须实现）

网页必须包含如下行为，避免“只显示失败但看不到原因”：

- 每个异步任务轮询需有超时（默认 20 分钟）
- 超时/失败时输出结构化错误详情，至少包含：
  - `phase`（create_draft/storyboard/tts/subtitle/image）
  - `taskId`（若有）
  - `url`（若有）
  - `httpStatus`（若为 HTTP 失败）
  - `response` 或 `taskData`（最后一次响应）

## 触发指令样例（供其他 AI 直接套用）

当用户说：

“开发一个儿童绘本的网页”

你应当直接生成一个可运行的 `website/index.html`，并满足：

- 左侧：主题 + token 输入 + 执行按钮 + 步骤状态
- 右侧：分镜 JSON 文案、音频链接、字幕时间轴、图片链接列表
- 第 4 步：图片按字幕时间从 0 连续铺满到末尾，并且为前置图片随机加转场
- 出错必须在页面上显示错误详情
