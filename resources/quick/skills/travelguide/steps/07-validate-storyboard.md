# 步骤 7：校验分镜与口播匹配（已合并至步骤 6）

> ⚠️ **本步骤已合并至步骤 6.5**。分镜生成后必须立即自检，无需单独执行本步骤。

## 说明

原步骤 7 的语义匹配校验逻辑已整合到 `steps/06-build-storyboard.md` 的 6.5 自检规则中。

执行时：
- 步骤 6 生成 `storyboard.json` 后，自检规则 5 已完成语义匹配校验
- **不再需要单独调用 LLM 进行二次校验**
- 直接进入步骤 8

## 保留的校验维度（供步骤 6.5 参考）

| 校验项 | 规则 |
|--------|------|
| 时间连续性 | 首段 start=0，每段 start=上段 end，末段 end=总时长 |
| 素材范围 | source_start ≥ 0，source_end ≤ 素材总时长 |
| 分镜时长 | 1.5s ≤ (end - start) ≤ 3.5s |
| 语义匹配 | description 与 subtitle_match 语义一致 |
